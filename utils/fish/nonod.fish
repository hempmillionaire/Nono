complete -c nonod -f

complete -c nonod -l help -d "Produce help message"
complete -c nonod -l version -d "Output version information"
complete -c nonod -l config-file -r -d "Specify configuration file"
complete -c nonod -l detach -d "Run as daemon"
complete -c nonod -l pidfile -r -F -d "File path to write the daemon's PID to (optional, requires --detach)"
complete -c nonod -l non-interactive -d "Run non-interactive"
complete -c nonod -l log-file -r -F -d "Specify log file"
complete -c nonod -l log-level -r -d ""
complete -c nonod -l max-log-file-size -r -d "Specify maximum log file size [B]. Default: 104850000"
complete -c nonod -l max-log-files -r -d "Specify maximum number of rotated log files to be saved (no limit by setting to 0). Default: 50"
complete -c nonod -l max-concurrency -r -d "Max number of threads to use for a parallel job. Default: 0"
complete -c nonod -l proxy -r -d "Network communication through proxy: <socks-ip:port> i.e. \"127.0.0.1:9050\""
complete -c nonod -l proxy-allow-dns-leaks -d "Allow DNS leaks outside of proxy"
complete -c nonod -l public-node -d "Allow other users to use the node as a remote (restricted RPC mode, view-only commands) and advertise it over P2P"
complete -c nonod -l zmq-rpc-bind-ip -r -d "IP for ZMQ RPC server to listen on. Default: 127.0.0.1"
complete -c nonod -l zmq-rpc-bind-port -r -d "Port for ZMQ RPC server to listen on. Default: 18082, 28082 if 'testnet', 38082 if 'stagenet'"
complete -c nonod -l zmq-pub -r -d "Address for ZMQ pub - tcp://ip:port or ipc://path "
complete -c nonod -l restricted-zmq-rpc -d "Restrict ZMQ RPC by disabling some sensitive methods; does not guarantee filtering of sensitive data"
complete -c nonod -l confirm-zmq-rpc-external-bind -d "Confirm zmq-rpc-bind-ip value is NOT a loopback (local) IP"
complete -c nonod -l no-zmq -d "Disable ZMQ RPC server [114/349]"
complete -c nonod -l data-dir -x -a "(__fish_complete_directories)" -d "Specify data directory"
complete -c nonod -l testnet -d "Run on testnet. The wallet must be launched with --testnet flag."
complete -c nonod -l stagenet -d "Run on stagenet. The wallet must be launched with --stagenet flag."
complete -c nonod -l regtest -d "Run in a regression testing mode."
complete -c nonod -l keep-fakechain -d "Don't delete any existing database when in fakechain mode."
complete -c nonod -l fixed-difficulty -r -d "Fixed difficulty used for testing. Default: 0"
complete -c nonod -l enforce-dns-checkpointing -d "checkpoints from DNS server will be enforced"
complete -c nonod -l prep-blocks-threads -r -d "Max number of threads to use when preparing block hashes in groups. Default: 4"
complete -c nonod -l fast-block-sync -r -d "Sync up most of the way by using embedded, known block hashes. Default: 1"
complete -c nonod -l show-time-stats -r -d "(=0) Show time-stats when processing blocks/txs and disk synchronization. Default: 0"
complete -c nonod -l block-sync-size -r -d "(=0) How many blocks to sync at once during chain synchronization (0 = adaptive). Default: 0"
complete -c nonod -l check-updates -x -a "disabled notify download update" -d "Check for new versions of NONO. Default: notify"
complete -c nonod -l no-fluffy-blocks -d "Relay blocks as normal blocks"
complete -c nonod -l test-dbg-lock-sleep -r -d "Sleep time in ms, defaults to 0 (off), used to debug before/after locking mutex. Values 100 to 1000 are good for tests."
complete -c nonod -l offline -d "Do not listen for peers, nor connect to any"
complete -c nonod -l disable-dns-checkpoints -d "Do not retrieve checkpoints from DNS"
complete -c nonod -l block-download-max-size -r -d "Set maximum size of block download queue in bytes (0 for default)"
complete -c nonod -l sync-pruned-blocks -d "Allow syncing from nodes with only pruned blocks"
complete -c nonod -l max-txpool-weight -r -d "Set maximum txpool weight in bytes. Default: 648000000"
complete -c nonod -l block-notify -r -d "Run a program for each new block, '%s' will be replaced by the block hash"
complete -c nonod -l prune-blockchain -d "Prune blockchain"
complete -c nonod -l reorg-notify -r -d "Run a program for each reorg, '%s' will be replaced by the split height, '%h' will be replaced by the new blockchain height, '%n' will be replaced by the number of new blocks in the new chain, and '%d' will be replaced by the number of blocks discarded from the old chain"
complete -c nonod -l block-rate-notify -r -d "Run a program when the block rate undergoes large fluctuations. This might be a sign of large amounts of hash rate going on and off the Monero network, and thus be of potential interest in predicting attacks. %t will be replaced by the number of minutes for the observation window, %b by the number of blocks observed within that window, and %e by the number of blocks that was expected in that window. It is suggested that this notification is used to automatically increase the number of confirmations required before a payment is acted upon."
complete -c nonod -l keep-alt-blocks -d "Keep alternative blocks on restart"
complete -c nonod -l extra-messages-file -r -F -d "Specify file for extra messages to include into coinbase transactions"
complete -c nonod -l start-mining -r -d "Specify wallet address to mining for"
complete -c nonod -l mining-threads -r -d "Specify mining threads count"
complete -c nonod -l bg-mining-enable -d "Enable background mining"
complete -c nonod -l bg-mining-ignore-battery -d "If true, assumes plugged in when unable to query system power status"
complete -c nonod -l bg-mining-min-idle-interval -r -d "Specify min lookback interval in seconds for determining idle state"
complete -c nonod -l bg-mining-idle-threshold -r -d "Specify minimum avg idle percentage over lookback interval"
complete -c nonod -l bg-mining-miner-target -r -d "Specify maximum percentage cpu use by miner(s)"
complete -c nonod -l db-sync-mode -r -d "Specify sync option, using format [safe|fast|fastest]:[sync|async]:[<nblo cks_per_sync>[blocks]|<nbytes_per_sync> [bytes]]. Default: fast:async:250000000bytes"
complete -c nonod -l db-salvage -d "Try to salvage a blockchain database if it seems corrupted"
complete -c nonod -l p2p-bind-ip -r -d "Interface for p2p network protocol (IPv4). Default: 0.0.0.0"
complete -c nonod -l p2p-bind-ipv6-address -r -d "Interface for p2p network protocol (IPv6). Default: ::"
complete -c nonod -l p2p-bind-port -r -d "Port for p2p network protocol (IPv4). Default: 18080, 28080 if 'testnet', 38080 if 'stagenet'"
complete -c nonod -l p2p-bind-port-ipv6 -d "Port for p2p network protocol (IPv6). Default: 18080, 28080 if 'testnet', 38080 if 'stagenet'"
complete -c nonod -l p2p-use-ipv6 -d "Enable IPv6 for p2p"
complete -c nonod -l p2p-ignore-ipv4 -d "Ignore unsuccessful IPv4 bind for p2p"
complete -c nonod -l p2p-external-port -r -d "External port for p2p network protocol (if port forwarding used with NAT). Default: 0"
complete -c nonod -l allow-local-ip -d "Allow local ip add to peer list, mostly in debug purposes"
complete -c nonod -l add-peer -r -d "Manually add peer to local peerlist"
complete -c nonod -l add-priority-node -r -d "Specify list of peers to connect to and attempt to keep the connection open"
complete -c nonod -l add-exclusive-node -r -d "Specify list of peers to connect to only. If this option is given the options add-priority-node and seed-node are ignored"
complete -c nonod -l seed-node -r -d "Connect to a node to retrieve peer addresses, and disconnect"
complete -c nonod -l tx-proxy -r -d "Send local txes through proxy: <network-type>,<socks-ip:port>[,max_con nections][,disable_noise] i.e. \"tor,127.0.0.1:9050,100,disable_noise\""
complete -c nonod -l anonymous-inbound -r -d "<hidden-service-address>,<[bind-ip:]port>[,max_connections] i.e. \"x.onion,127.0.0.1:18083,100\""
complete -c nonod -l ban-list -r -F -d "Specify ban list file, one IP address per line"
complete -c nonod -l hide-my-port -d "Do not announce yourself as peerlist candidate"
complete -c nonod -l no-sync -d "Don't synchronize the blockchain with other peers"
complete -c nonod -l enable-dns-blocklist -d "Apply realtime blocklist from DNS"
complete -c nonod -l out-peers -r -d "Set max number of out peers. Default: 12"
complete -c nonod -l in-peers -r -d "Set max number of in peers. Default: -1"
complete -c nonod -l tos-flag -r -d "Set TOS flag. Default: -1"
complete -c nonod -l limit-rate-up -r -d "Set limit-rate-up [kB/s]. Default: 8192"
complete -c nonod -l limit-rate-down -r -d "Set limit-rate-down [kB/s]. Default: 32768"
complete -c nonod -l limit-rate -r -d "Set limit-rate [kB/s]. Default: -1"
complete -c nonod -l pad-transactions -d "Pad relayed transactions to help defend against traffic volume analysis"
complete -c nonod -l max-connections-per-ip -r -d "Maximum number of connections allowed from the same IP address. Default: 1"
complete -c nonod -l rpc-bind-port -r -d "Port for RPC server. Default: 18081, 28081 if 'testnet', 38081 if 'stagenet'"
complete -c nonod -l rpc-restricted-bind-port -r -d "Port for restricted RPC server"
complete -c nonod -l restricted-rpc -d "Restrict RPC to view only commands and do not return privacy sensitive data in RPC calls"
complete -c nonod -l bootstrap-daemon-address -r -d "URL of a 'bootstrap' remote daemon that the connected wallets can use while this daemon is still not fully synced. Use 'auto' to enable automatic public nodes discovering and bootstrap daemon switching"
complete -c nonod -l bootstrap-daemon-login -r -d "Specify username:password for the bootstrap daemon login"
complete -c nonod -l bootstrap-daemon-proxy -r -d "<ip>:<port> socks proxy to use for bootstrap daemon connections"
complete -c nonod -l rpc-bind-ip -r -d "Specify IP to bind RPC server. Default: 127.0.0.1"
complete -c nonod -l rpc-bind-ipv6-address -r -d "Specify IPv6 address to bind RPC server. Default: ::1"
complete -c nonod -l rpc-restricted-bind-ip -r -d "Specify IP to bind restricted RPC server. Default: 127.0.0.1"
complete -c nonod -l rpc-restricted-bind-ipv6-address -r -d "Specify IPv6 address to bind restricted RPC server. Default: ::1"
complete -c nonod -l rpc-use-ipv6 -d "Allow IPv6 for RPC"
complete -c nonod -l rpc-ignore-ipv4 -d "Ignore unsuccessful IPv4 bind for RPC"
complete -c nonod -l rpc-login -d "Specify username[:password] required for RPC server"
complete -c nonod -l confirm-external-bind -d "Confirm rpc-bind-ip value is NOT a loopback (local) IP"
complete -c nonod -l rpc-access-control-origins -r -d "Specify a comma separated list of origins to allow cross origin resource sharing"
complete -c nonod -l rpc-ssl -x -a "enabled disabled autodetect" -d "Enable SSL on RPC connections. Default: autodetect"
complete -c nonod -l rpc-ssl-private-key -r -k -a "(__fish_complete_suffix .key)" -d "Path to a PEM format private key"
complete -c nonod -l rpc-ssl-certificate -r -k -a "(__fish_complete_suffix .crt)" -d "Path to a PEM format certificate"
complete -c nonod -l rpc-ssl-ca-certificates -r -F -d "Path to file containing concatenated PEM format certificate(s) to replace system CA(s)."
complete -c nonod -l rpc-ssl-allowed-fingerprints -r -d "List of certificate fingerprints to allow"
complete -c nonod -l rpc-ssl-allow-chained -d "Allow user (via --rpc-ssl-certificates) chain certificates"
complete -c nonod -l disable-rpc-ban -d "Do not ban hosts on RPC errors"
complete -c nonod -l rpc-ssl-allow-any-cert -d "Allow any peer certificate"
complete -c nonod -l rpc-max-connections-per-public-ip -d "Max RPC connections per public IP permitted. Default: 3"
complete -c nonod -l rpc-max-connections-per-private-ip -d "Max RPC connections per private and localhost IP permitted. Default: 25"
complete -c nonod -l rpc-max-connections -d "Max RPC connections permitted. Default: 100"
complete -c nonod -l rpc-response-soft-limit -d "Max response bytes that can be queued, enforced at next response attempt. Default: 26214400"
